я хочу написать приложение которое будет похожа на CRM и jira, но будет завязано на отслеживание неисправностей + база знаний про неиспраностям и проектам в которых они возникают. Неисправности возникают при эксплуатации разных систем автоматического управления (проекты). Неисправности должны добавляться вручную, редактироваться и т.д. Создай актуальный стэк технологии (для backend используй python 3.8.10 + uv). для фронтенда Jinja2. это должно работать в браузере. ОС - windows 7. мне нужна дорожная карта и это будет мой пет проект, в котором я буду учиться. я прикрепил примерную UX структуру:  дашборд с (неисправности + база знаний+ проекты). Kanban доска (по статусам Fault) — через react-beautiful-dnd Комментарии + история (как лента в Jira) Фильтры (по системе, severity, назначенному) Автоэскалация (Celery: каждые 10 мин проверяет sla_deadline и поднимает severity)/ подбери правильную архитектуру

Примерный стек-технологий, если надо критикуй или подбери актуальное

Актуальный стек (Python first, браузер)
Слой	Технология	Почему
Бэкенд	        FastAPI (асинхронный)	            Быстрый, автодока, WebSocket из коробки
ORM	            S 1.4.x + Alembic               	Работа с БД, миграции
База	        SQLite → PostgreSQL	                SQLite для разработки, PostgreSQL для прода
Авторизация 	JWT, python-jose                    Для JWT сессий и брокера задач
Фон. задачи	    BackgroundTasks (FastAPI)→ Celery   Уведомления, неисправности → перейти на Celery позже
Стейт-менеджментZustand (легче Redux)	            Для фильтров и текущего пользователя
Контейнеры	    Docker + docker-compose	            Поднимаете всё одной командой
Шаблоны	        Jinja2 + Bootstrap 5	            Работает на Windows 7, без Node.js
Интерактивность	Vanilla JS + HTMX (опционально)	    Можно обновлять части страницы без React


Примерная структура проекта

39_Faults/
└── backend/                          # Корневая папка бэкенда
    ├── app/                          # Весь код приложения
    │   ├── api/                      # API-роутеры (ваши существующие)
    │   │   ├── __init__.py
    │   │   ├── dashboard.py          # Новый роутер для главной страницы
    │   │   ├── faults.py             # Ваш существующий
    │   │   ├── knowledge_base.py     # Ваш существующий
    │   │   └── projects.py           # Ваш существующий
    │   ├── core/                     # Ядро (конфиг, БД, безопасность)
    │   │   ├── __init__.py
    │   │   ├── config.py
    │   │   ├── database.py
    │   │   └── security.py
    │   ├── models/                   # SQLAlchemy модели
    │   │   ├── __init__.py
    │   │   ├── all_models.py         # Можно оставить или разбить на отдельные файлы
    │   │   ├── fault.py
    │   │   └── project.py
    │   ├── schemas/                  # Pydantic схемы
    │   │   ├── __init__.py
    │   │   ├── fault.py
    │   │   └── project.py
    │   ├── services/                 # Бизнес-логика
    │   │   ├── __init__.py
    │   │   ├── escalation.py
    │   │   ├── notifications.py
    │   │   └── search.py
    │   ├── static/                   # НОВО! Статические файлы (CSS, JS, изображения)
    │   │   ├── css/
    │   │   │   └── style.css         # Ваши кастомные стили
    │   │   └── js/
    │   │       └── app.js            # Ваш JavaScript для интерактивности
    │   ├── templates/                # НОВО! HTML-шаблоны Jinja2
    │   │   ├── base.html             # Базовый шаблон с общей структурой
    │   │   ├── dashboard.html        # Главная страница с дашбордом
    │   │   ├── projects.html         # Список проектов
    │   │   ├── faults.html           # Список неисправностей
    │   │   └── fault_detail.html     # Детальная страница неисправности
    │   ├── main.py                   # Точка входа в приложение
    │   └── __init__.py
    ├── migrations/                   # Миграции Alembic (не меняется)
    ├── scripts/                      # Вспомогательные скрипты (не меняется)
    │   ├── init_backend_env.sh
    │   ├── run.sh
    │   └── toexe.sh
    ├── tests/                        # Тесты (не меняется)
    ├── .python-version               # Версия Python (не меняется)
    ├── faults.db                     # Файл SQLite базы (не меняется)
    ├── init_db.py                    # Скрипт инициализации БД (не меняется)
    ├── migrate_db.py                 # Скрипт миграции БД (не меняется)
    ├── pyproject.toml                # Зависимости uv (не меняется)
    ├── requirements.txt              # Зависимости pip (не меняется)
    ├── seed_data.py                  # НОВО! Скрипт для наполнения тестовыми данными
    ├── uv.lock                       # Lock-файл uv (не меняется)
    ├── Readme.md                     # Документация (не меняется)
    └── _version.py                   # Версия проекта (не меняется)
